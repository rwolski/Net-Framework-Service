using System.Collections.Generic;

namespace Framework.Data
{
    public interface IStorage
    {
        //void Save(object obj);

        //void Delete(object obj);

        //object FindByIdentity(object id);

        //object FindFirstOrDefault(WhereCondition<T> whereConditions = null, IEnumerable<OrderBy<T>> orderBy = null);

        //IEnumerable<object> FindAll();

        //IEnumerable<object> Find(WhereCondition<T> whereConditions = null, IEnumerable<OrderBy<T>> orderBy = null, int? limit = null);
    }
}